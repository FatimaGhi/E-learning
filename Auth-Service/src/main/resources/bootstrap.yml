spring:
  application:
    name: Auth-Service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: FILES
        prefixes: config
        default-context: application